# mobile-app

Flutter mobile application for Sikizwa Voice 360°.

## Run

```bash
cd apps/mobile-app
flutter pub get
flutter run
```

## Notes

- Uses Riverpod for state management.
- Uses GoRouter for navigation.
- Update API base URL in `lib/src/services/api_service.dart` for production.
